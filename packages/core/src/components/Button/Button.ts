import { ButtonProps } from './types';

/**
 * Button
 *
 * This is a declarative placeholder for a Button component.
 * It should be replaced at build time by an AI agent or build system.
 *
 * @generationMeta
 * {
 *   "generator": "./generate",
 *   "prompt": "./prompt",
 *   "promptExport": "prompt",
 *   "types": "./types"
 * }
 */
export const Button = (props: ButtonProps & { children?: unknown }) => {
  if (process.env.NODE_ENV !== 'production') {
    throw new Error('Button is a declarative component. It must be replaced at build time by the build system.');
  }

  return null;
};
